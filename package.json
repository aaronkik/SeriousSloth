{
  "name": "serioussloth",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test:ci": "turbo run test:ci",
    "prepare": "husky"
  },
  "devDependencies": {
    "husky": "9.1.7"
  },
  "engines": {
    "node": "20.x"
  },
  "packageManager": "bun@1.3.13",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
