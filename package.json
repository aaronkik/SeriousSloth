{
  "name": "serioussloth",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test:ci": "turbo run test:ci",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.2"
  },
  "engines": {
    "node": "20.x"
  },
  "packageManager": "bun@1.3.12",
  "workspaces": [
    "apps/*",
    "packages/*"
  ]
}
